fn main():
    let name = "Otter"
    let tagline = name + "Lang" + " makes strings easy"
    println(tagline)

    let score = "Score: " + 42
    println(score)

    let reversed = 7 + " seas"
    println(reversed)